=== use below commands to generate keystore in your classpath ===

keytool -genkey -keyalg RSA -alias selfsigned -keystore <absolute-path-for-your-resources-folder>/keystore.jks -storepass root123 -validity 360 -keysize 2048